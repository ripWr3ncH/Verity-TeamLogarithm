{
  "name": "verity-exposure-chaincode",
  "version": "0.1.0",
  "private": true,
  "description": "VERITY exposure channel — Paillier aggregation and proof-checked ceremony results",
  "main": "dist/src/index.js",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc",
    "start": "fabric-chaincode-node start",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fabric-contract-api": "^2.5.4",
    "fabric-shim": "^2.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2"
  }
}
